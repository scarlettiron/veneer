packages:
  - 'packages/*'
  - 'examples/*'

allowBuilds:
  better-sqlite3: false
  esbuild: false

# Allow only these dependencies to run their install build scripts.
# esbuild needs this so tsup can build the packages.
onlyBuiltDependencies:
  - esbuild
